apiVersion: v1
kind: Service
metadata:
  name: notifications
  namespace: sh-ops
  labels:
    app: notifications
spec:
  selector:
    app: notifications
  ports:
    - name: http
      port: 80
      targetPort: http
