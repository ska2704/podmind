apiVersion: v1
kind: Service
metadata:
  name: billing
  namespace: sh-ops
  labels:
    app: billing
spec:
  selector:
    app: billing
  ports:
    - name: http
      port: 80
      targetPort: http
