apiVersion: apps/v1
kind: Deployment
metadata:
  name: room
  namespace: sh-core
  labels:
    app: room
    podmind.component: smarthostel-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: room
  template:
    metadata:
      labels:
        app: room
        podmind.component: smarthostel-service
    spec:
      containers:
        - name: room
          image: nginx:alpine
          ports:
            - name: http
              containerPort: 80
          env:
            - name: SERVICE_NAME
              value: room
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          readinessProbe:
            httpGet: { path: /, port: http }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /, port: http }
            periodSeconds: 10
          volumeMounts:
            - name: data
              mountPath: /var/lib/room
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: room
