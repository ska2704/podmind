apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: sh-core
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
    - name: http
      port: 80
      targetPort: http
