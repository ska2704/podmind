apiVersion: v1
kind: Service
metadata:
  name: sensor-ingest
  namespace: sh-edge
  labels:
    app: sensor-ingest
spec:
  selector:
    app: sensor-ingest
  ports:
    - name: http
      port: 80
      targetPort: http
