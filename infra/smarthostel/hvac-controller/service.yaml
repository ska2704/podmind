apiVersion: v1
kind: Service
metadata:
  name: hvac-controller
  namespace: sh-edge
  labels:
    app: hvac-controller
spec:
  selector:
    app: hvac-controller
  ports:
    - name: http
      port: 80
      targetPort: http
