apiVersion: v1
kind: Service
metadata:
  name: coordinator
  namespace: podmind
  labels:
    app: coordinator
spec:
  selector:
    app: coordinator
  ports:
    - name: http
      port: 8000
      targetPort: http
